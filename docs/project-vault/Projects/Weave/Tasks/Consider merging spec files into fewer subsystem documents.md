---
type: task
project: '[[Projects/Weave/Project]]'
epic: '[[Epics/Epic-dogfood-vault-migration]]'
status: backlog
category: loose-end
rank: 3000
---

# Consider merging spec files into fewer subsystem documents

Deliberately left undone. The roadmap's own governing-document lists show a
median of three to four specs per slice, and the two most-cited specs —
dependencies and agent access — pair with different neighbors in each slice,
so a subsystem merge would relocate the cost rather than remove it.

Revisit only if `area` frontmatter selection proves insufficient in practice.
